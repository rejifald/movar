# Code owners for movar. Listing an owner here auto-requests their review on
# matching PRs. Today this is advisory only: the repo has a single maintainer,
# and GitHub blocks self-approval, so the branch ruleset requires 0 approvals
# (see docs/metrics-gate.md). When a second maintainer joins, flip
# `require_code_owner_review` on in the ruleset to make owner review mandatory.
#
# Keep bot / agent accounts OUT of this file — code-owner review is one of the
# human-only gates.

* @rejifald

# The «Для української» directory's entries. Called out separately because it
# is the one path in this repo that expects pull requests from people outside
# it — a project asking to be listed, or correcting how it was described.
#
# This line does not gate anything on its own (see the note above: the ruleset
# requires 0 approvals while there is one maintainer). The guard that does the
# work for this path is .github/workflows/directory-submission-guard.yml, which
# fails a fork PR that edits these entries *and* anything else.
apps/marketing/src/content/projects/ @rejifald
