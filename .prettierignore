# Dependency, build, and generated output.
dist
.output
.wxt
.nx
node_modules
pnpm-lock.yaml
coverage
*.tsbuildinfo

# Real-world HTML fixtures must remain byte-preserving for parser regressions.
**/*.fixture.html

# Generated by scripts/gen-theme-css.mts from @movar/theme's typed tokens —
# prettier would fight the generator's formatting (and the check:theme guard).
packages/theme/styles

# Generated by scripts/fallow-targets.mts on every `pnpm metrics` run. Prettier
# realigns its markdown tables, which the generator un-aligns on the next run —
# an edit war that surfaced as churn in every PR, and that defeats the
# generator's "skip the write when nothing substantive changed" check.
docs/REFACTORING-QUEUE.md
